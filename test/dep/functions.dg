include "../common.dg"

extern fun main() C_int {
    foo(2)
    // expect: foo
    // expect: bar
    // expect: foo
    // expect: bar
    
    return 0
}

fun foo(i: Int) {
    if i < 0 {
        return
    }
    io::println("foo")
    bar(i-1)
}

fun bar(i: Int) {
    io::println("bar")
    foo(i-1)
}