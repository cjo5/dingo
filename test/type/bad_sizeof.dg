include "../common.dg"

fun[c] main() i32 {
    putiln(sizeof(void)) // expect-error: type void does not have a size

    val a i32
    putiln(sizeof(a)) // expect-error: 'a' is not a type

    return 0
}