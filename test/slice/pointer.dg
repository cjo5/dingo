include "../common.dg"

fun[c] main() c_int {
    var data = 9
    var ptr = &var data
    val slice = &var ptr[:1]

    putiln(len(slice)) // expect-output: 1
    putiln(slice[0]) // expect-output: 9

    slice[0]++
    putiln(data) // expect-output: 10

    return 0
}