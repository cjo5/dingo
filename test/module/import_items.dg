include "../common.dg"

extern fun main() i32 {
    printiln(foo) // expect: 10
    printiln(bar()) // expect: 100
    local() 
    // expect: 20
    // expect: 200
    return 0
}

import _ = mod1: foo = bar, bar = baz

fun local() {
    import _ = mod2: (foo = bar, bar = baz)
    printiln(foo)
    printiln(bar())
}