pub module mod1 {
    pub import mod2

    pub struct Foo {
        pub var a: Int
    }

    pub val bar: Int = 10

    pub fun baz() Int {
        return 100
    }
}

pub module foo {
    pub module bar {
        fun baz() {

        }
    }
}