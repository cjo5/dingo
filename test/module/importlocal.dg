include "../common.dg"

importlocal foo: pub bar
importlocal foo

extern fun main() c_int {
    foo.bar() // expect: foo
    bar() // expect: foo
    return 0
}

module foo {
    pub fun bar() {
        .println(modfqn_)
    }
}