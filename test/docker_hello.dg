include "common.dg"

fun[c] main() i32 {
    puts(c"This message was generated by a Dingo program inside Docker.")
    puts(c"You can compile and execute a different Dingo program by adding it as an argument to docker run.")
    puts(c"Note: the path must be relative to Dockerfile and located in either test/ or examples/.")
    return 0
}