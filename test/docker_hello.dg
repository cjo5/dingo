include "common.dg"

fun[c] main() c_int {
    putln("This message was generated by a Dingo program inside Docker.")
    putln("You can compile and execute a different Dingo program by adding it as an argument to docker run.")
    putln("Note: the path must be relative to Dockerfile and located in either test/ or examples/.")
    return 0
}