include "common.dg"

extern fun main() c_int {
    putln("This message was generated by a Dingo program inside Docker.\n")
    putln("Compile and execute a different program by passing the following command as argument to docker run:")
    putln("    /bin/bash -c \"dgc FILES && ./dgexe [ARGS]\"")
    putln("\nNote: files must be relative to Dockerfile and located in either test/ or examples/.")
    return 0
}