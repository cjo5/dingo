include "common.dg"

extern fun main() c_int {
    io::println("This message was generated by a Dingo program inside Docker.\n")
    io::println("Compile and execute a different program by passing the following command as argument to docker run:")
    io::println("    /bin/bash -c \"dgc FILES && ./dgexe [ARGS]\"")
    io::println("\nNote: files must be relative to Dockerfile and located in either test/ or examples/.")
    return 0
}