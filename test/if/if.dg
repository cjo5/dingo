include "../common.dg"

fun[c] main() i32 {
    if true {
        puts(c"true") // expect-exe: true
    }

    if !false {
        puts(c"!false") // expect-exe: !false
    }

    val a = true
    if a {
        puts(c"a") // expect-exe: a
    }

    val b = false
    if !b {
        puts(c"!b") // expect-exe: !b
    }

    val c = fun() bool {
        return true
    }

    if c() {
        puts(c"c") // expect-exe: c
    }

    val d = fun() bool {
        return false
    }

    if !d() {
        puts(c"!d") // expect-exe: !d
    }

    val foo = Foo{a: true, b: false}

    if foo.a {
        puts(c"foo.a") // expect-exe: foo.a
    }

    if !foo.b {
        puts(c"!foo.b") // expect-exe: !foo.b
    }

    val bar = [true, false]

    if bar[0] {
        puts(c"bar[0]") // expect-exe: bar[0]
    }

    if !bar[1] {
        puts(c"!bar[1]") // expect-exe: !bar[1]
    }

    return 0
}

struct Foo {
    a bool
    b bool
}