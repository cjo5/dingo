include "std/io.dg"

struct Foo {
    f i32
}

fun geta() [5:i32] {
    return [1, 2, 3, 4, 5]
}

fun getb() Foo {
    var f Foo
    return f
}

pub fun main() {
    var a [5:i32]
    var b *var [:i32] = &var a[1:]
    var c = &var b[2:4]
    c[0] = 4

    puti(a[3])
    puti(b[2])
    puti(c[0])

    puts("".ptr)

    puti(a.len)
    puti(b.len)
    puti(c.len)

    if (&a[3]) == (&c[0]) {
        puts("true".ptr)
    }
}