include "libc.dg"

fun putln(str &[i8]) {
    put(str)
    putchar('\n')
}

fun putiln(i i32) {
    puti64(i)
    putchar('\n')
}

fun putbool(b bool) {
    if b {
        put("true\n")
    } else {
        put("false\n")
    }
}

fun put(str &[i8]) {
    for i usize = 0; i < len(str); i++ {
        putchar(str[i])
    }
}

fun puti32(i i32) {
    puti64(i)
}

fun puti64(i i64) {
    if i < 0 {
        putchar('-')
        putu64(-i as u64)
    } else {
        putu64(i as u64)
    }
}

fun putu64(i u64) {
    if i >= 10 {
        putu64(i/10)
    }
    putchar(('0' + i%10) as i32)
}
